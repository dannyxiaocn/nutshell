# nutshell_dev_codex — Implementation

## Usage

```bash
nutshell new --entity nutshell_dev_codex
nutshell chat --entity nutshell_dev_codex "review this subsystem"
```

## Overrides

- `model`: openai-codex/gpt-5.4
- `provider`: codex-oauth
- Own memory files (Codex-specific)

Everything else inherited from `nutshell_dev`.
