# Agent (`agenthub/agent/`) — Design

The default general-purpose agent. Base template that other agents extend.

## Purpose

- Default prompts for persistent file-backed work
- Default built-in tool schemas: `bash`, `skill`, `web_search`
- The base `creator-mode` skill
- Runtime defaults for the whole agent family
