# Porter System Tests — Design

Centralized porter-managed pytest coverage for subsystem structure, layout, and tree-level behavior.

## Naming Convention

```
test_<component>_<version>_<topic>.py
```

This keeps porter-managed suites visually separate from feature-local pytest files.
