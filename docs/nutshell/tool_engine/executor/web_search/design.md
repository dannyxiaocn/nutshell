# Web Search Executor — Design

Swappable search backends behind a single logical `web_search` tool. The rest of the runtime sees one tool; this directory provides the interchangeable implementations.
