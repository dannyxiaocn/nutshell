# Skill Executor — Design

The skill tool is the **progressive disclosure mechanism** for `SKILL.md`. The skill engine advertises available skills in the prompt catalog; this executor loads the full body only when the agent requests it.
