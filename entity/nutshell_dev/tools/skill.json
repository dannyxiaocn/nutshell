{
  "name": "skill",
  "description": "Load a skill into context before working on a task that matches an available skill.",
  "input_schema": {
    "type": "object",
    "properties": {
      "skill": {
        "type": "string",
        "description": "The skill name to load."
      },
      "args": {
        "type": "string",
        "description": "Optional raw argument string for parameterized skills."
      }
    },
    "required": ["skill"]
  }
}
