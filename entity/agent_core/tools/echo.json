{
  "name": "echo",
  "description": "Return the input text unchanged. Useful for testing tool call routing.",
  "input_schema": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "The text to echo back."
      }
    },
    "required": ["text"]
  }
}
