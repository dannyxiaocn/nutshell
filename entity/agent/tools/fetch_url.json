{
  "name": "fetch_url",
  "description": "Fetch a URL and return its text content. Strips HTML to readable text. Use after web_search to read the full content of a relevant page, or to access documentation, APIs, and data files directly.",
  "input_schema": {
    "type": "object",
    "properties": {
      "url": {
        "type": "string",
        "description": "The URL to fetch (http or https)."
      },
      "max_chars": {
        "type": "integer",
        "description": "Maximum characters to return (default: 8000). Longer content is truncated."
      }
    },
    "required": ["url"]
  }
}
