{
  "name": "web_search_tavily",
  "description": "Search the web using Tavily Search. Returns titles, URLs, and snippet content. Requires TAVILY_API_KEY environment variable. Supports only 'query' and 'count'.",
  "input_schema": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Search query string."
      },
      "count": {
        "type": "integer",
        "description": "Number of results to return (1-10). Default: 5.",
        "minimum": 1,
        "maximum": 10
      }
    },
    "required": ["query"]
  }
}
