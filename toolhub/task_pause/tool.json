{
  "name": "task_pause",
  "description": "Pause a task card (works for recurring OR one-shot pending cards). It stops firing until you call `task_resume`.",
  "input_schema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Name of the task card to pause."
      }
    },
    "required": ["name"]
  }
}
